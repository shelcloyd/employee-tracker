SELECT 
    department_name AS department
FROM department
LEFT JOIN department

SELECT
    job_title AS title,
FROM role
LEFT JOIN role

SELECT
    manager_id AS manager
FROM employee
LEFT JOIN employees